/** @type {import('tailwindcss').Config} */
export default __CONFIG__
module.exports = {
    content: [
        // Specify paths to your template files
    ],
    theme: {
        extend: {
            colors: {
                // Custom blue color
                customBlue: '#1E40AF', // Example blue color
            },
        },
    },
    plugins: [require('daisyui')],
    daisyui: {
        themes: [
            {
                mytheme: {
                    "primary": "#1E40AF", // Setting primary color to blue
                    "secondary": "#3B82F6", // Example secondary color
                    "accent": "#60A5FA", // Example accent color
                    // Add more color customizations as needed
                },
            },
            // You can include other themes here
        ],
    },
    plugins: [require('daisyui')],
};